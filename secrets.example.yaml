# Copy this file to secrets.local.yaml and replace the placeholders locally.
# Never commit secrets.local.yaml or secrets.yaml.
api_key: "replace-with-your-api-key"
base_url: "https://api.openai.com/v1"
