# 后端 API 根地址（后续对接 Studio BFF 时填写）
# VITE_API_BASE_URL=http://127.0.0.1:8000

# ZhiXing 兵工厂元数据（流程模板、插件列表等）。
# - npm run dev：可不设，前端默认请求同源 ``/zhixing-studio``，由 Vite 转到本机 8765。
# - 直连 Python Studio：填根地址即可，例如下面一行（不要写成 …8765/zhixing-studio，否则会 404）。
# VITE_STUDIO_API_BASE=http://127.0.0.1:8765
