/**
 * 兵工厂浮动面（属性面板 / 底栏容器）样式类名，定义于 `globals.css` 的 `--studio-builder-toolbar-*` 变量。
 * 改主题时只调 CSS 变量即可（第五章 5.2）。
 */
export const STUDIO_BUILDER_FLOATING_SURFACE_CLASS = "studio-builder-floating-surface";
