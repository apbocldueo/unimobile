# 知行
随着多模态大模型（MLLM）的爆发，Mobile Agent 领域涌现了大量优秀工作。然而，现有的研究往往面临**开发碎片化**、**组件强耦合**以及**生态割裂**的三大挑战。为了复现一个 SOTA 算法，研究者往往需要重写大量底层代码；为了对比不同模型的效果，开发者缺乏一个统一的实验环境。

**知行 (ZhiXing)** 旨在为此提供一个工业级的基础设施。我们不定义“什么是最好的 Agent”，我们定义 **“Agent 该如何被科学地构建与评测”**。

> 我们的愿景：从一个灵活的开发框架出发，知行将逐步演进为涵盖 **自动化数据生成 (Data Generation)**、**标准化全链路评测 (Standardized Evaluation)** 的全栈智能体科研平台。



## 🔥 核心特性

我们专注于提供最稳固的工程底座，让算法研究无后顾之忧。

### 1. 真正的架构级解耦

我们解决了微调模型与通用模型接口不统一的工程难题。

- **Universal Brain & Planner**：通过独创的 **Preset (预设)** 机制，我们将 Prompt 模板、模型调用与结果解析器（Parser）动态绑定。
- **Model-Agnostic**：框架不绑定任何特定模型。你可以在同一个架构下，自由混用 **GPT-4o**（通用推理）与本地微调的 **Small Language Model**，无需修改核心代码。

### 2.  极致的配置驱动

- **零代码组装**：通过 YAML 文件即可定义 Agent 的全貌。想要对比 "OmniParser vs Grid" 的感知差异？只需修改配置文件中的一行参数。
- **环境隔离**：支持针对不同设备、不同任务加载不同的配置模板，极大提升了实验管理的效率。



## 🏗️ 顶层架构

UniMobile 采用五层分层架构设计，以支持最大的灵活性和扩展性。

![architecture.png](asset/architecture.png)

### 层级详解

- **L5 Application**: 配置加载器、开发者 SDK、Evaluation 入口。
- **L4 Data & Eval**: *(Research Preview)* 负责参数化数据的供给与评测标准的定义。
- **L3 Strategies**: 智能体策略层，负责调度组件（如 ModularAgent, AppAgentAdapter）。
- **L2 Components**: 核心组件库，包含 Perception, Brain, Memory, Planner。
- **L1 Device**: 硬件抽象层，统一管理 Harmony 和 Android Device。

*注：实线部分为 v0.1 已实装模块，虚线部分为 v1.0/v2.0 规划中模块。*



## 🧩 组件菜单

v0.1 版本已内置以下核心组件，支持自由组合：

| 模块 (Module)  | 预设 / 模式 (Preset/Mode) | 说明                                                         |
| :------------- | :------------------------ | :----------------------------------------------------------- |
| **Brain**      | `general_vlm`             | **通用视觉模型**：适配 GPT-4V/Claude，支持图文理解与 JSON 动作输出。 |
| **Planner**    | `manager_style`           | **思维链规划**：生成分步计划，适合复杂逻辑任务。             |
|                | `mobimind_style`          | **结构化规划**：直接输出目标 App 与意图，适合明确指令。      |
| **Perception** | `omniparser`              | **语义解析**：生成结构化的 UI 元素列表（文本+坐标）。        |
|                | `grid`                    | **网格定位**：经典的通用兜底方案，不依赖元素识别。           |
|                | `set_of_marks`            | **视觉打标**：使用检测模型给 UI 元素打上数字标签，大幅提高点击准确率。 |
| **Memory**     | `sliding_window`          | **滑动窗口**：保留最近 N 步操作，平衡 Token 消耗。           |
|                | `summary_memory`          | **摘要记忆**：周期性压缩历史记录，保留长程关键信息。         |



## 🚀 快速开始

### 1. 安装依赖

```Bash
# 1. 创建并激活虚拟环境
conda create -n unimobile python=3.10
conda activate unimobile

# 2. 克隆项目
git clone https://github.com/YourLab/UniMobile.git
cd UniMobile

# 3. 安装项目依赖
pip install -r requirements.txt
```

### 2. 部署感知服务

UniMobile 的默认视觉感知依赖于 **OmniParser**。你需要先启动 OmniParser 的模型服务。访问 [OmniParser 官方仓库](https://github.com/microsoft/OmniParser)，按照要求部署模型。

> 💡 **Tip**: 如果你不想部署庞大的模型，可以使用 set_of_marks 或 grid 进行轻量级体验。

### 3. 连接设备

知行 支持 Android 和 Harmony 双平台。

#### **连接 Android 设备**

1. 开启手机的 **“开发者选项”** -> **“USB 调试”**。

2. 通过 USB 连接电脑。

3. 验证连接：

   ```Bash
   adb devices
   ```

#### **连接 Harmony 设备**

1. 确保已安装鸿蒙 SDK 工具链 (hdc)。

2. 开启手机的 **“USB 调试”**。

3. 验证连接：

   ```Bash
   hdc list targets
   ```

### 4. 配置文件

我们通过 YAML 定义 Agent。以下是一个 **"鸿蒙高精架构"** 的配置示例：

```yaml
# 设备配置
device:
  name: "harmony"

agent:
  # 策略
  strategy: "modular_agent"

  params:
    verbose: true

  # 组件配置
  components:
    # --- 感知器 ---
    perception:
      - name: "omniparser"
        params:
          url: "http://your-server:your-port"
        
    # --- 大脑 ---
    brain:
      name: "universal_brain"
      params:
        preset: "general_vlm"
      llm:
        name: "openai_llm"
        params:
          api_key: "your-api-key"
          base_url: "your-base-url"         
          model: "gpt-4o"
    
    # --- 记忆 ---
    memory:
      name: "sliding_window"
      params: { window_size: 6 }

    # --- 规划器 ---
    planner:
      name: "universal_planner" 
      params:
        preset: "manager_style"  
```

### 5. 运行 Demo

为了展示 UniMobile 架构的灵活性，我们构建了两个截然不同的 Agent 变体。它们分别运行在 **Android** 和 **Harmony** 上，虽然执行相似的视频搜索任务，但其**内部架构完全不同**。

**场景 A：经典架构 Agent (Android)**

- **配置组合**：Manager Planner (分步拆解) +  Grid Perception (网格感知) + Sliding Window Memory
- **特点**：模拟了通用大模型处理手机任务的标准范式，依赖强大的逻辑拆解能力。

```Bash
# 任务：在 YouTube 搜索 DFS 算法并播放第一个视频
python run.py --config configs/agent_android_classic.yaml \
  --task "Search for the 'dfs algorithm' using youtube and play the first video that appears"
```



https://github.com/user-attachments/assets/78d72bd9-e5fc-4e19-94b6-7b07a4eb5ec3


**场景 B：高精架构 Agent (HarmonyOS)**

- **配置组合**：MobiMind Planner (结构化决策) + OmniParser (语义感知) + Summary Memory
- **特点**：利用了更先进的屏幕解析器（OmniParser）和结构化输出，执行更加精准高效。

```Bash
# 任务：在 Bilibili 搜索 DFS 算法并播放播放量最高的视频
python run.py --config configs/agent_harmony_advanced.yaml \
  --task "使用bilibili搜索 dfs算法，并按照播放量排序选择播放量最高的"
```


https://github.com/user-attachments/assets/9f9628f2-46f4-465d-b689-7be5a2c133fe



## 🗺️ Roadmap

### **v0.1 - The Foundation**
- [x] **硬件层:** 完成 HarmonyOS/Android 双端统一封装
- [x] **应用层**：落地 Registry + ConfigLoader 核心引擎。
- [x] **组件生态**：集成 OmniParser, OpenAI LLM 等基础组件。

### **v1.0 - Enhancement**
- [ ] **Data Generation**: 基于 Template 的参数化任务生成器。
- [ ] **Knowledge Base**: 初步的 RAG 知识库支持。
- [ ] **Advanced Strategies**：实装 **Exploration**与 **Reflection** 闭环，实现 Agent 的自我进化
- [ ] **External Adapters**: 完整复现 AppAgent / Mobile-Agent v2 等 Mobile Agent 领域 SOTA 算法。
- [ ] **Dataset**: 发布标准化的评测数据集。
- [ ] **Developer SDK**: 开放组件注册接口，支持社区贡献自定义插件。



## 📄 协议

本项目遵循 [Apache License](./LICENSE) 开源协议。

## :star2: 致谢 & 引用

我们的设计灵感来自于社区优秀的开源工作：
- **[FlashRAG](https://github.com/RUC-NLPIR/FlashRAG)**: 其模块化的工具库设计哲学深深影响了本框架的架构。
- **[AppAgent](https://github.com/TencentQQGYLab/AppAgent)** & **[MobiAgent](https://github.com/IPADS-SAI/MobiAgent)**: 移动端 Agent 的先行者，启发了我们的组件划分。

如果这个框架对你的研究有帮助，请给我们点个 Star! 🌟
