# Studio screenshot provenance

Captured on 2026-09-14 from the running public `release-v2` application at
commit `ccb496bb707de248c24d2153a97e7e9cdb28fa1d`, using a separate local
SQLite database. Images are unedited 1280 × 720 browser captures.

- `home.png`: fresh workspace and authoring entry point.
- `create.png`: named Agent and official explicit-loop template.
- `graph.png`: created template graph after validation.
- `inspector.png`: selected Reasoning implementation and configuration.
- `saved.png`: saved revision restored after browser refresh.
- `readiness.png`: actual read-only runtime configuration diagnostics.

The walkthrough verified creation, inspection, validation, saving, and reload.
No Android task or model request was executed. Missing device and secret
configuration is intentional; the images do not establish runtime readiness.
Both language versions reuse the same actual application screenshots.
